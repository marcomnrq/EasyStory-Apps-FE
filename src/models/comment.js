
export default class Comment {
    constructor(id, content, postId, userId) {
        this.id = id;
        this.content = content;
        this.postId = postId;
        this.userId = userId;
    }
}
